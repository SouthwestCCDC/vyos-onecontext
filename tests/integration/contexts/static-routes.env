# Static routes test context
# Tests Phase 3 static routing functionality
#
# This validates:
# - Gateway-based routes (set protocols static route X next-hop Y)
# - Interface-based routes (set protocols static route X interface Y)
# - Routes in default VRF (main routing table)
#
# Note: In QEMU single-VM tests, routes won't be reachable but commands
# should be generated correctly.

HOSTNAME="test-static-routes"
SSH_PUBLIC_KEY="ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIC+Z0iQ4AaHmgc9OWxSBKJnkXtOa57N0AVMv8cJYWi+F test@integration"
ONECONTEXT_MODE="stateless"

# Management interface
ETH0_IP="192.168.122.60"
ETH0_MASK="255.255.255.0"
ETH0_GATEWAY="192.168.122.1"
ETH0_DNS="8.8.8.8"

# Static routes configuration
# - Default route via gateway (next-hop)
# - Specific network route via interface (blackhole/null route pattern)
ROUTES_JSON='{"static":[{"interface":"eth0","destination":"0.0.0.0/0","gateway":"192.168.122.1"},{"interface":"eth0","destination":"192.0.2.0/24"}]}'
