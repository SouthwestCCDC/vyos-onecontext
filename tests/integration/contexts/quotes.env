# Quote bug regression test context (issue #40)
#
# This fixture tests the quote handling bug reported in issue #40. The original
# bug occurred when SSH keys contained double quotes in the comment field
# (e.g., "admin@test" below). The parser incorrectly split the key value at the
# embedded quotes, causing configuration generation to fail.
#
# The SSH_PUBLIC_KEY value below intentionally contains double quotes in the
# comment field to verify the fix works correctly.

HOSTNAME="test-quotes"
SSH_PUBLIC_KEY='ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIC+Z0iQ4AaHmgc9OWxSBKJnkXtOa57N0AVMv8cJYWi+F "test@quotes"'
ONECONTEXT_MODE="stateless"

ETH0_IP="192.168.122.20"
ETH0_MASK="255.255.255.0"
ETH0_GATEWAY="192.168.122.1"
